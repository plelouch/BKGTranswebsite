{% extends 'admin/base.html.twig' %}

{% block title %}Edition de la Location numeros : {{ location.id }}{% endblock %}

{% block body %}
    <div class="container-fluid">
        <!-- Breadcrumbs-->
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="#">Modification des formaliters de location numeros : {{ location.id }}</a>
            </li>
            <li class="breadcrumb-item active"></li>
        </ol>

        <div class="card mb-3">
            <div class="card-header">
                <i class="fas fa-table"></i>
                Modification des formaliters de location numeros : {{ location.id }}
            </div>
            <div class="card-body">
                {{ include('admin/location/_fromProperty.html.twig', {'button_label': 'Modifier'}) }}
            </div>
        </div>
        <a class="btn btn-link" href="{{ path('admin_location_index') }}">
            <i class="fas fa-arrow-circle-left"></i>
            Retour à la liste
        </a>
    </div>
{% endblock %}
