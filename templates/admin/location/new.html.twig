{% extends 'admin/base.html.twig' %}

{% block title %}Nouvelle Location de voiture{% endblock %}

{% block body %}
    <div class="container-fluid">
        <!-- Breadcrumbs-->
        <ol class="breadcrumb">
            <li class="breadcrumb-item">
                <a href="#">Louer une voiture</a>
            </li>
            <li class="breadcrumb-item active"></li>
        </ol>

        <div class="card mb-3">
            <div class="card-header">
                Location
            </div>
            <div class="card-body">
                {{ include('admin/location/_form.html.twig') }}
            </div>
        </div>
        <a class="btn btn-link" href="{{ path('admin_location_index') }}">
            <i class="fas fa-arrow-circle-left"></i>
            Retour à la liste
        </a>
    </div>
{% endblock %}
